import React from 'react';

export default function NoMatch(){
    // the function will return "page  not found" when the question ID doesn't not exist.
    return(
        <h3>404 - Not found</h3>
    )
}